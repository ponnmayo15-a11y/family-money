# family-money — 親の介護と、子供の受験

親の介護・葬式と、子供の受験が重なったとき、兄弟で足りるかを見るウェブアプリ。
足りない分は、長男が全部出す場合と、長男・長女・次女の3人で分ける場合を並べる。
最初の画面で項目を押し、そのページだけ開く。最初の画面には金額や結果を出さない。
GitHub のコミットには Gmail を使わない。作者メールは `ponnmayo15-a11y@users.noreply.github.com`。

## コマンド一覧

```bash
npm start
```

ブラウザで `http://localhost:5174` を開く。
ウェブは `https://ponnmayo15-a11y.github.io/family-money/`（パソコンを閉じても開ける）。

## プロジェクト構成

```
family-money/
├── CLAUDE.md
├── package.json
├── index.html
├── css/style.css
├── js/
│   ├── data.js   ← 項目と調べ方のヒント
│   ├── calc.js   ← 合計・年ごとの費用・足りるか・分担
│   └── app.js    ← 画面の動き・保存
└── .cursor/skills/keep-family-money/
    └── SKILL.md
```

## 共通ルール

- ユーザーはプログラミング未経験者の場合がある。1ステップずつ案内する
- ユーザーにファイルを直接編集させない。必要な値はチャットで受け取り、エージェントが設定する
- エラーが起きたら、何が起きたか・次に何をすればいいかを平易に伝える

## やること / やらないこと

やること:
- 親の貯金・年金・保険・投資・家や土地・仕事場の売値・借金の手入力
- 介護・生活・病院（月）、葬式と家の片づけ（1回）。介護・病院・葬式は介護者ごとに分ける
- 介護・葬式・借金・片づけを、兄弟が援助するか選ぶ。用意できる額も人ごとに入れる
- 設定で、兄弟の人数と介護者の人数を変えられる
- 各入力画面に「その他」を足せる
- 分け方は「同じ額」「一番上が多め」「一番上がほぼ全部」
- 親の手元資金（貯金＋保険＋投資＋その他）と、売る前提の仕事場で足りるか
- 家は最初は売らない。見通しで入れる／入れないを選べる
- 空欄は「まだわからない」（ゼロ扱いしない）
- この端末への自動保存

やらないこと:
- 銀行との自動連携
- 年によって違う費用（今は毎年同じ）
- 固定資産税の評価額をそのまま売値にする
- 姉への請求や連絡の自動化
- ネット上の会員登録・ログイン
- 家族のスマホへ数字が自動で同じになる仕組み

家族の前提: 初期は長男1人、姉2人。人数は画面で変えられる。

---

## 起動スキル

```bash
npm start
```

完了の定義: ブラウザで入力画面が開く。

---

## 調整スキル

`.cursor/skills/keep-family-money/SKILL.md` を読んで実行する。
